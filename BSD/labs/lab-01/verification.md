# Lab 01: Verifikasi

## Checklist

- `uname -a` menampilkan FreeBSD 14.x
- `ifconfig` menampilkan interface dengan alamat IP (DHCP)
- `pkg update` berjalan tanpa error
- `service sshd status` menunjukkan sshd aktif
- Login via SSH dari host ke VM berhasil (jika network diatur)
