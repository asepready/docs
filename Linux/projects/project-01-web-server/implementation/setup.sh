#!/bin/bash
# setup.sh - Deploy web server (Nginx, PHP-FPM, MySQL/PostgreSQL)
set -e
echo "=== Web server setup (placeholder) ==="
# apt install nginx php-fpm mysql-server (atau dnf)
# Konfigurasi, systemctl enable --now
exit 0
