# Cheat Sheet: Basic Commands (Linux)

- apt install/update/upgrade (dnf install/update)
- systemctl start/stop/restart/enable
- ls, cp, mv, rm, mkdir, chmod, chown
- cat, less, head, tail, grep, find
- man, which, type

*(Dapat dikonversi ke PDF.)*
