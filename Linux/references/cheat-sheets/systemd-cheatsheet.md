# Cheat Sheet: systemd

- systemctl status/start/stop/restart/enable/disable
- journalctl -u unit -f -b
- systemctl list-units, list-unit-files
- Unit file: [Unit], [Service], [Install]
- systemctl daemon-reload

*(Dapat dikonversi ke PDF.)*
