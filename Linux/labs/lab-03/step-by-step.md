# Lab 03: Langkah

1. Buat group (e.g. developers)
2. useradd -m -s /bin/bash -G developers user1
3. Set permission pada satu direktori (chmod, chown)
4. Tambahkan user ke sudo: usermod -aG sudo user1 (Ubuntu) atau wheel (RHEL)
