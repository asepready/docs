# Lab 01: Verifikasi

## Checklist

- `uname -a` menampilkan Linux (Ubuntu/Rocky)
- `ip a` menampilkan interface dengan alamat IP
- `sudo apt update` atau `sudo dnf update` berjalan tanpa error
- `systemctl status ssh` menunjukkan active
- Login via SSH dari host ke VM berhasil
